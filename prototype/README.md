# PCA Prototype

Reference implementation of the PUNN Cognitive Architecture (PCA).

Current Version:

Prototype v0.1


## Run

From the repository root, install the prototype dependencies and run:

```bash
python -m prototype.app
```